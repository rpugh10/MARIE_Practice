loopConditions, Load i
				Subt Five
				Skipcond 400
				Jump loopBody
				Jump end

loopBody, Input
		  Store Value //Storing user input in value
		  Load Value //Loading Value into the AC
		  Subt Large // Subtracting by Large
		  SkipCond 800 //Skip next instruction if AC > 0
		  Jump loopIncrement //Jump to loop increment (this instruction will be skipped if condition is true)
		  Load Value //Loading value into AC
		  Store Large //Storing large in value
		  Jump loopIncrement


loopIncrement, Load i
	   		   Add One
			   Store i
			   jump loopConditions


end, Load Large
	 Output
	 Halt



Large, DEC 0
Five, DEC 5
i, DEC 0
One, DEC 1
Value, DEC 0
